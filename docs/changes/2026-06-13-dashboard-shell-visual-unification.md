# 2026-06-13 工作台壳层视觉统一记录

## 背景

- 用户要求把除个人中心外的前端工作台页面，统一到和“品牌增长策略”一致的左侧菜单与页面排版风格。
- 用户同时明确要求：
  - 其他页面左侧菜单保留一级菜单，不要人为增加二级菜单。
  - 去掉某书、某音/某号页头顶部的“回到品牌增长策略”和“查看个人中心”按钮。

## 本次改动

### 1. 统一工作台外层骨架

- 将以下页面的外层容器统一到和品牌增长策略一致的 `archive-shell strategy-shell` 骨架：
  - `xiaohongshu`
  - `douyin`
  - `wechat`
  - `more-features`
  - `more-features/design`
- 统一使用同一套 `strategy-layout`、`strategy-content-panel` 和 `strategy-page-header` 排版节奏。

### 2. 统一左侧一级菜单样式

- 所有上述工作台页面的左侧菜单都改为使用品牌增长策略同款目录面板视觉：
  - 同款目录容器边框、圆角、背景渐变和阴影
  - 同款一级按钮高度、内边距、圆角和激活态
- 菜单信息架构保持一级菜单，不再引入手风琴二级菜单。

### 3. 移除多余页头跳转按钮

- 在某书和某音/某号页面中，移除页头顶部的：
  - `回到品牌增长策略`
  - `查看个人中心`
- 保留当前页面自身需要的状态提示与刷新动作。

### 4. 顶部主导航延续当前命名

- 顶部主导航继续沿用本轮之前已确认的命名调整：
  - `小红书` -> `某书`
  - `抖音` -> `某音/某号`
  - 去掉 `视频号`
  - 去掉 `私域`

## 结果

- 除个人中心外，各工作台页面的左侧菜单视觉与品牌增长策略保持统一。
- 工作台页面整体外层排版统一，页面进入后不再出现明显不同的壳层风格。
- 某书、某音/某号页头中的跨页快捷按钮已移除，顶部操作更聚焦当前工作区。
