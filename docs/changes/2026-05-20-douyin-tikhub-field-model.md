# 2026-05-20 抖音 Tikhub 字段模型内化

## 背景

- 原计划分两步处理 Tikhub 样例字段甄别和页面搭建。
- 本次直接完成字段甄别与前端模型内化。

## 本次完成

- 在前端 `collectors.ts` 中补充抖音账号、抖音作品的标准类型定义。
- 追加抖音账号与作品的示例数据，用于页面字段承载预演。
- 将抖音板块从“空态占位”升级为：
  - 字段预览表
  - 标准示例卡
  - 统计补丁说明
- 新增仓库文档 `docs/douyin-tikhub-field-mapping.md`，记录字段来源、原始路径和补丁规则。

## 风险边界

- 本次仍未接入真实抖音接口调用。
- 不改 API 协议，不改数据库 schema。
- 页面内展示的抖音示例卡仅用于字段模型验证，不代表真实线上数据。

## 后续续接

- 下一步可直接用真实 Tikhub 接口结果替换示例数据。
- 后端接入时建议优先复用现有 `fetchTikHub` 能力，并按 `aweme_id` 合并统计补丁。
