# [OPEN] digital-human-502-list

## 症状
- 数字人模块仍然异常，页面提示模板列表不可用，出现 `502 Bad Gateway`。
- 需要确认问题发生在前端请求、Next 代理、Nest 接口，还是上游蝉镜接口。

## 当前假设
- 假设 1：前端数字人首页加载时请求了错误路由或错误参数，导致代理层直接返回 502。
- 假设 2：Next `/api/[...path]` 代理对数字人模板列表请求头或响应处理异常，502 发生在 Web 层。
- 假设 3：Nest 数字人模板列表接口调用蝉镜上游失败，但没有被正确降级，最终透传为 502。
- 假设 4：当前品牌的第三方平台配置缺失或读取错配，导致只有数字人模板列表接口报错。
- 假设 5：页面首屏并发请求中某个数字人相关接口抛错，前端把它统一映射成“模板列表不可用”。

## 计划
- 先对前端数字人首页加载、Web 代理、Nest 数字人模板列表接口增加运行时埋点。
- 复现并读取日志，排除错误假设。
- 基于证据做最小修复，再做一次 post-fix 对比验证。
