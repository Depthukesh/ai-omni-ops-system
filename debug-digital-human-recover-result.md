# [OPEN] digital-human-recover-result

## 症状

- 用户反馈数字人作品页面中，打叉标记的冗余区域需要删除。
- 用户反馈某个 3 点多开始创作的数字人作品到现在仍显示“生成中”。
- 用户反馈“找回视频结果”按钮点击后没有反应，当前无法有效找回作品结果。
- 需要同时记录本次数字人列表接口的性能优化进度。

## 当前假设

- 假设 1：前端“找回视频结果”按钮没有真正绑定到恢复请求，或者点击后被条件分支直接返回。
- 假设 2：前端请求发出了，但缺少 `workId`、`providerTaskId` 或品牌参数，后端无法定位要恢复的作品。
- 假设 3：后端恢复接口已执行，但 `findRecoverableDigitalHumanWorkRow(...)` 没有命中该作品，导致恢复逻辑提前失败。
- 假设 4：第三方作品实际已经生成成功，但本地快照没有被刷新回写，所以页面一直停留在“生成中”。
- 假设 5：页面被用户打叉的内容来自数字人作品详情区域的静态信息块，删除它不会影响找回逻辑，但需要单独做 UI 收口。

## 已知上下文

- 数字人作品列表接口已在本轮优化为“本地快照立即返回 + 后台异步刷新”。
- 当前需要继续下钻的是“找回视频结果”按钮链路，以及特定作品为什么长期停留在生成中。

## 计划

1. 先读取前端数字人作品详情与“找回视频结果”按钮绑定代码。
2. 再定位后端数字人恢复接口、参数要求和失败路径。
3. 只加埋点，不先改业务逻辑。
4. 收到线上复现证据后，再做最小修复。

## 当前证据

- 前端按钮已确认真实绑定 `recoverDouyinDigitalHumanVideo(...)`，不是点击无事件。
- 用户线上复现后，“找回视频结果”和“按任务 ID 找回”都返回同一条提示：`上游服务暂时不可用（502 Bad Gateway），请稍后重试`。
- 前端该提示并非业务侧明确返回，而是 `apps/web/src/services/http.ts` 在收到 HTML/502 响应时统一翻译出的网关文案。
- 后端恢复链路原本会同步执行：
  - `recoverDouyinDigitalHumanVideo(...)`
  - `queryDigitalHumanVideoSnapshot(...)`
  - `chanjingOpenApiService.getVideoDetail(...)`
- 这说明两个按钮虽然入口参数不同，但最终都会收敛到同一条第三方查询链路，因此统一报 502 更符合“同步等待上游导致网关打断”的模式。

## 当前结论

- 假设 1：前端按钮没有真正发请求。
  - 结论：排除。
- 假设 2：请求参数缺少 `workId` / `providerTaskId`。
  - 结论：暂未完全排除，但不是当前两个按钮统一 502 的主因。
- 假设 3：后端恢复接口命中了错误作品或没命中作品。
  - 结论：仍保留，但当前更像后半段上游查询失败。
- 假设 4：第三方作品实际已完成，但恢复接口同步查上游过慢，被网关打断。
  - 结论：当前最接近证据。

## 已执行修复

- 将数字人“找回视频结果”从同步等待第三方，调整为：
  - 先回写站内“找回中”状态；
  - 再后台异步查询蝉镜结果并持久化；
  - 前端收到响应后给出“后台同步中”的提示，并自动延迟刷新列表。
- 用户继续线上验证后，新的错误已收敛为：
  - `上传 OSS 文件失败`
  - `Response timeout for 60000ms`
- 这说明：
  - 蝉镜结果大概率已经成功拿到；
  - 当前失败点不再是“找回结果接口被 502 打断”，而是“回填成品视频到 OSS 时超时失败”。
- 已继续收口 OSS 存储层：
  - 小文件保持普通上传；
  - 大文件改为分片上传；
  - OSS 请求超时从默认 60 秒提升到 5 分钟；
  - 对超时类错误追加有限重试。
