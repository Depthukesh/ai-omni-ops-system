# [OPEN] digital-human-silent-upload

## 背景

- 症状：创建数字人上传训练视频后，页面没有明显反馈，任务中心为空。
- 目标：确认请求是否真正提交、服务端是否创建任务、首页与任务中心为何都未回显。

## 假设

1. 前端创建请求并未真正发出，或提交后被静默吞掉。
2. 服务端创建流程已执行，但在任务落库前失败，前端没有拿到明确错误。
3. 任务已创建，但绑定到了错误的用户或品牌上下文，导致任务中心查不到。
4. 数字人本地快照已写入，但首页列表接口过滤或合并逻辑继续漏掉待处理记录。
5. 蝉镜上游请求在某个步骤返回异常，服务端未把失败态持久化到任务或数字人快照中。

## 当前计划

1. 给前端提交动作和服务端关键步骤加运行时埋点。
2. 复现一次上传流程并收集日志。
3. 根据证据决定是修前端静默提交、任务落库、用户归属还是列表回显链路。
